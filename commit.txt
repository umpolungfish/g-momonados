I retain decoded inputs and phase-history values as T/F cell strings in the V⊙x-compiled membrane, and verify the default base-sweep artifact. The 682-digit run closes in 42.20 seconds and the 10+ digit asymmetric semiprime closes in 26.05 seconds under `vox run`; both returned Gödel pairs match their known factors. Rust orchestration remains outside this runtime path.
