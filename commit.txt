I compile encoded inputs into contained phase factor binaries and run phase selection, signed bit-register cancellation, paired return, and product closure through the IMASM arithmetic circuits. I keep one square-and-reduce circuit resident, test the returned factors and wider complement words with warnings denied, and record the one-minute phase-orbit boundary.
