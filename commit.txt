I add streaming IMASM subtraction beside encoded addition

I add a pure IMASM ripple-borrow program with an explicit final borrow cell,
and expose it through the encoded `imasm_sub` command. Inputs retain the
canonical ⊥=1, ⊤=0 mapping; arithmetic and borrow propagation stay in the
instruction stream. I cover every borrow row and a 300-bit subtraction in
tests. The full binary suite passes.
