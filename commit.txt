I fuse the encoded support read and binary GCD into one resident IMASM circuit, reuse the support scratch registers for the GCD, and keep the complementary residual as pair closure. I test both support layouts, record the 404-digit closure and 682-digit timeout, and preserve the baked trial binaries.
