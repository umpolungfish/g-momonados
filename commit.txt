I remove the baked-word argument ceiling by generating the C include from complete IMASM numeral words. I run the expanded balanced and wider semiprime battery through `vox run`, retain its baked membranes and logs, compare sparse and single-base schedules, and record the one-minute cutoffs alongside the prior successful closures.
