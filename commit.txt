I add a dynamically sized IMASM ripple adder

I express bitwise addition as a pure ParaASM instruction stream with B4 input
cells, an IMASM carry register, and no fixed-width arithmetic operation. I
verify every full-adder truth-table row and a 300-bit carry chain. I also pass
the newly required optional tape directory in the two phase-unbraid tests so
the binary test target builds again.
