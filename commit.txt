I move the phase extractor's complete control flow into a V⊙x-compiled, glyph-contained membrane. It decodes baked Gödel words, uses width-scaled registers and a growable phase map, selects support reads, sweeps bases, times, reports errors, and emits factors only at residual closure. The every-phase 682-digit semiprime closes in 42.71 seconds under `vox run`; the 10+ digit asymmetric semiprime closes in 26.99 seconds. I keep the Rust prototype separate and unchanged.
