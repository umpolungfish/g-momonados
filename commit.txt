I transport arithmetic values across Gödel evaluation frames and register each return closure at widths 2–8. I preserve the decimal source as an IMASM word, operate on values selected from distinct frames, reframe the result, and read it back. The RSA battery remains open until its factors close.
