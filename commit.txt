I remove the fixed sieve aperture ceiling and use dynamic precision, segmented storage, and an arbitrary-precision path. I verify the expanded RSA frame sweep and document the computational cost without claiming factor recovery.
