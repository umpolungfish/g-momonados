I replace the factor-return GCD divider with a looping IMASM split, shift, cancellation, and multiplicative rejoin. I test the encoded circuit across small and wide operands and bake contained semiprime trials.
