I isolate baked phase seeds and support-read cadence for the 682-digit run. Single-base trials at 2, 3, 4, and N−1 all reach the fixed one-minute timeout; reading support at every phase also times out. I record two repeat 404-digit closures, document dynamic frame sizing and the remaining performance ambiguity, and preserve the diagnostic binaries.
