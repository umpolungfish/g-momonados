I make the phase-frame factor readout feed an odd-anchor complement whose zero residual closes the pair itself. I evaluate support in width-eight IMASM frames, compile the contained membranes for release, and record the wider closed trials.
